node {
    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'credentials-id') {

        def customImage = docker.build("db", "-f django2/clone/Dockerfile .")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
